const Sequelize = require('sequelize')
/// Notice this is based on my DB password and might raise u errors (change the 1234):
const db = new Sequelize('mysql://root:1234@localhost/sunday_finalProject') 

const dummyData = []



const loadTask = (task) => {
    
}
